[project]
name = "trackitall-frontend"
version = "0.1.0"
description = "TrackItAll frontend — Streamlit client for the API"
requires-python = ">=3.12"
dependencies = [
    "requests>=2.34.2",
    "streamlit>=1.60.0",
]
