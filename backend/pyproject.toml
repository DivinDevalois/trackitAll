[project]
name = "trackitall-backend"
version = "0.1.0"
description = "TrackItAll backend — API and analytics layer"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.139.2",
    "uvicorn[standard]>=0.51.0",
]
